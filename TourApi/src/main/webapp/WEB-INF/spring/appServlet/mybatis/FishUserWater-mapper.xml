<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.biz.tour.dao.userwater.FishUserWaterDao">

	<insert id="insert" parameterType="fishUserWaterVO">
		insert into
		tbl_userfish_water(
		uf_username,
		uf_title,
		uf_date,
		uf_text
		) values (
		#{uf_username,jdbcType=VARCHAR},
		#{uf_title,jdbcType=VARCHAR},
		#{uf_date,jdbcType=VARCHAR},
		#{uf_text,jdbcType=VARCHAR}
		)
	</insert>
	<update id="update" parameterType="fishUserWaterVO">
		update tbl_userfish_water set
		B_WRITER=#{b_writer,jdbcType=VARCHAR},
		B_SUBJECT
		=#{b_subject,jdbcType=VARCHAR},
		B_CONTENT
		=#{b_content,jdbcType=VARCHAR},
		B_FILE=#{b_file,jdbcType=VARCHAR}
		where
		B_ID =#{b_id,jdbcType=VARCHAR}
	</update>
</mapper> 