<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.biz.tour.dao.userwater.FishUserWaterPicsDao">

	<insert id="insert" parameterType="FishUserWaterPicsVO">
		insert into
		tbl_userfish_water_pics(
		ufp_fk,
		ufp_originalFName,
		ufp_uploadedFName
		) values (
		#{ufp_fk,jdbcType=VARCHAR},
		#{ufp_originalFName,jdbcType=VARCHAR},
		#{ufp_uploadedFName,jdbcType=VARCHAR}
		)
	</insert>
	<update id="update" parameterType="FishUserWaterPicsVO">
		update tbl_userfish_water_pics set
		B_WRITER=#{b_writer,jdbcType=VARCHAR},
		B_SUBJECT
		=#{b_subject,jdbcType=VARCHAR},
		B_CONTENT
		=#{b_content,jdbcType=VARCHAR},
		B_FILE=#{b_file,jdbcType=VARCHAR}
		where
		B_ID =#{b_id,jdbcType=VARCHAR}
	</update>
</mapper> 